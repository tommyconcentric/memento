<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Merged into the generated Info.plist (GENERATE_INFOPLIST_FILE stays
	     YES). Only keys with no INFOPLIST_KEY_ build-setting equivalent
	     belong here. remote-notification lets CloudKit push changes to the
	     app in the background, so cross-device sync doesn't wait for the
	     next launch. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
	<!-- Registers Memento as an (alternate-rank) opener of plain text, so an
	     AirDropped profile card — a structured .txt so Notes and anyone
	     without Memento can still read it — offers "Open in Memento" on the
	     receiving device. RootView's onOpenURL parses the card and offers to
	     add the person; non-profile text files are declined with an alert. -->
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Text Document</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>LSHandlerRank</key>
			<string>Alternate</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>public.plain-text</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
